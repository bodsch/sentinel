# Downloaded blackbox_exporter, built benchtool, and per-run artefacts.
.cache/
run/
logs/
results.txt

# benchtool built by hand from this directory (`go build` here names the binary
# after the package directory). run.sh builds into .cache/ instead.
/bench
